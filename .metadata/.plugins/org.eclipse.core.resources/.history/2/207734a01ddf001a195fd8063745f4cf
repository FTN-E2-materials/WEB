package dao;

import java.util.List;

public interface DAO<T, ID> {
	List<T> getAll();
	T getById(ID id);
	T create(T entity);
	T update(T entity);
	boolean delete(T entity);
	void save(T entity);
	void saveAll(List<T> entities);
}
